# Milestone v1.2: Product Features + UX Unification

**Status:** ✅ SHIPPED 2026-04-17
**Phases:** 14-20
**Total Plans:** 17

## Overview

v1.2 delivered leads management and adopter/public UX unification for production tenant flows, then closed human-verification and phase-verification audit gaps to complete requirements traceability for milestone signoff.

## Phases

### Phase 14: Leads Backend

**Goal**: Shelter admins and adopters can exchange lead data through a working API with complete tenant isolation
**Depends on**: Nothing (pure backend, no frontend dependency)
**Requirements**: LEAD-01, LEAD-02, LEAD-03
**Success Criteria** (what must be TRUE):
  1. A curl POST to `/api/v1/leads` with name + email (no auth token) creates a lead record in RDS and returns 201 with a lead ID
  2. A curl GET to `/api/v1/admin/leads` with a valid JWT returns only leads belonging to the authenticated tenant — leads from other tenants are never present in the response
  3. A curl GET to `/api/v1/admin/leads/{id}` returns full contact info, questionnaire JSON, and match scores for a lead belonging to the requester's tenant; returns 404 for a lead belonging to a different tenant
  4. The lead list endpoint returns summary fields only (name, contact, date, status) — questionnaire JSON and scores are absent from the list response, preventing Lambda 6 MB limit breach
  5. An isolation test (cross-tenant query) is written and passing in the test suite before Phase 15 begins
**Plans**: 4 plans

Plans:
- [x] 14-01-PLAN.md — Build leads data foundation (status migration + schemas + tenant-safe CRUD/service)
- [x] 14-02-PLAN.md — Implement and wire public/admin leads API endpoints
- [x] 14-03-PLAN.md — TDD verification for lead API contracts and cross-tenant isolation
- [x] 14-04-PLAN.md — Close LEAD-01 persistence contract gap with nullable email migration and phone-only regression coverage

### Phase 15: Admin Leads Panel

**Goal**: Shelter admins can see who has shown interest in their animals and review full adopter profiles
**Depends on**: Phase 14
**Requirements**: ADMU-01, ADMU-02
**Success Criteria** (what must be TRUE):
  1. Logged-in admin navigates to the leads section and sees a table with name, contact info, date, and a status badge (New / Contacted / Adopted / Rejected) for each lead
  2. Admin filters the list by status and the table updates to show only matching leads without a full page reload
  3. Admin clicks a lead row and arrives at a detail page showing full contact info, all questionnaire answers with human-readable labels (not raw JSON keys), and compatibility scores for each matched animal
  4. The admin panel deploys as an EC2 static push and is accessible at `{slug}.matchcota.tech/admin/leads` in production
**Plans**: 2 plans
**UI hint**: yes

Plans:
- [x] 15-01-PLAN.md — Define leads data contracts (API helper + questionnaire label/group utility)
- [x] 15-02-PLAN.md — Implement admin leads list/detail UI, route/nav wiring, and production verification checkpoint

### Phase 16: Adopter Lead Capture

**Goal**: Adopters who see their match results can leave contact info that is stored together with their questionnaire and scores
**Depends on**: Phase 14
**Requirements**: ADOP-01, ADOP-02, ADOP-03
**Success Criteria** (what must be TRUE):
  1. An adopter who completes the questionnaire always sees their match results first — the contact capture form is never shown before results render, and results are never gated on form submission
  2. The capture form appears inline below the results, requires name plus at least one of email or phone, and allows all other fields to be left blank
  3. After submitting the form the adopter sees a distinct confirmation screen acknowledging their interest
  4. The lead record stored in RDS contains the adopter's questionnaire responses and match scores — it is not a contact-only record missing match context
  5. `MatchTest.jsx` passes questionnaire responses in React navigate state so `MatchResults.jsx` can include them in the POST body
**Plans**: 2 plans
**UI hint**: yes

Plans:
- [x] 16-01-PLAN.md — Establish public lead submit API helper + payload utilities and route-state handoff
- [x] 16-02-PLAN.md — Implement results-first inline lead form + confirmation flow and behavior tests

### Phase 17: UI Palette Normalization

**Goal**: Every page of the platform — public and admin — uses a single consistent primary colour with no teal or blue outliers
**Depends on**: Nothing (isolated cosmetic change, no cross-phase dependency)
**Requirements**: PAL-01
**Success Criteria** (what must be TRUE):
  1. `tailwind.config.js` defines `primary` as the indigo family (`#4F46E5` DEFAULT, `#4338CA` dark, `#6366F1` light) and no other primary-like tokens remain
  2. A manual visual check of all public pages (`/`, `/animals`, `/test`, `/test/results`) and all admin pages (`/admin`, `/admin/animals`, `/admin/leads`, `/admin/settings`) shows no teal or blue-600 elements — all interactive elements and CTAs use the indigo token
  3. The palette change ships as an isolated PR from feature work; no leads or questionnaire functionality is affected by the diff
**Plans**: 3 plans
**UI hint**: yes

Plans:
- [x] 17-01-PLAN.md — Remap Tailwind primary token to indigo and normalize shared UI primitive states
- [x] 17-02-PLAN.md — Migrate public/auth/layout palette outliers with behavior-safe visual checkpoint
- [x] 17-03-PLAN.md — Sweep admin/animal component outliers and run full frontend palette regression gate

### Phase 18: Tenant Public UX Unification

**Goal:** Tenant-facing public pages adopt a premium, unified visual language aligned with Landing/RegisterTenant/Admin references while preserving all existing behavior and route contracts
**Requirements**: UX18-01, UX18-02, UX18-03, UX18-04, UX18-05
**Depends on:** Phase 17
**Success Criteria** (what must be TRUE):
  1. Tenant public shell (`/home`, `/animals`, `/animals/:id`, `/test`, `/test/results`) presents a consistent premium design language with layered/tinted surfaces, rounded elevated cards, and stronger typography hierarchy
  2. `/test/results` clearly emphasizes the top match as a hero card while secondary matches are visually smaller in a responsive compact grid
  3. Results-first flow remains strict: full results render before lead-capture form with no behavior/order regression
  4. `/animals` uses a premium editorial card-grid with sticky filter bar and modern segmented pager while preserving existing filtering and pagination logic
  5. Any overlooked tenant-facing public route discovered during implementation receives the same premium unification pass in this phase
**Plans:** 3 plans
**UI hint**: yes

Plans:
- [x] 18-01-PLAN.md — Upgrade public shell + shared UI primitives to define premium component language
- [x] 18-02-PLAN.md — Redesign animals list/detail experience with sticky premium filters and preserved behavior
- [x] 18-03-PLAN.md — Rework match hierarchy (hero + compact secondary grid) and sweep remaining public pages

### Phase 19: Human Verification Gate Closure

**Goal:** Convert human-needed partials into verified closure for production admin leads flow and full palette consistency flow
**Requirements**: ADMU-01, ADMU-02, PAL-01
**Depends on:** Phases 15 and 17
**Gap Closure:** Closes audit requirement partials and flow gaps tied to unresolved human verification gates
**Success Criteria** (what must be TRUE):
  1. Human UAT confirms `https://{slug}.matchcota.tech/admin/leads` and lead detail navigation are reachable and readable in production for at least one tenant slug
  2. Human UAT confirms questionnaire answer readability and interaction quality on admin lead detail pages meet requirement intent
  3. Human visual sweep confirms palette consistency across required public/admin routes with no teal or blue outliers
  4. Verification evidence is captured in phase artifacts and each mapped requirement can be marked complete again with auditable proof
**Plans**: 2 plans

Plans:
- [x] 19-01-PLAN.md — Closed admin leads human verification gate with production/UAT evidence and verification updates
- [x] 19-02-PLAN.md — Closed palette visual gate with route sweep evidence and requirement re-validation

### Phase 20: Phase 18 Verification Backfill + UX18 Closure

**Goal:** Restore the missing Phase 18 verification link and close all orphaned UX18 requirements with explicit evidence
**Requirements**: UX18-01, UX18-02, UX18-03, UX18-04, UX18-05
**Depends on:** Phases 18 and 19
**Gap Closure:** Closes audit requirement orphaning, integration chain break (execution summary -> verification), and unsatisfied premium UX flow evidence
**Success Criteria** (what must be TRUE):
  1. A new Phase 18 verification artifact exists and verifies all UX18 route-level criteria with route-by-route evidence
  2. UX18 requirements are re-linked to verification proof so no orphaned requirements remain in milestone traceability
  3. Premium tenant public UX flow (`/home`, `/animals`, `/animals/:id`, `/test`, `/test/results`) is explicitly verified end-to-end with results-first behavior preserved
  4. Milestone integration chain from plan -> summary -> verification is complete for the former Phase 18 gap surface
**Plans**: 1 plan

Plans:
- [x] 20-01-PLAN.md — Backfilled Phase 18 verification artifact and closed UX18 orphaned requirements with automated + human evidence

### Progress Table

| Phase | Plans Complete | Status | Completed |
|-------|----------------|--------|-----------|
| 14. Leads Backend | 4/4 | Complete | 2026-04-15 |
| 15. Admin Leads Panel | 2/2 | Complete | 2026-04-16 |
| 16. Adopter Lead Capture | 2/2 | Complete | 2026-04-16 |
| 17. UI Palette Normalization | 3/3 | Complete | 2026-04-16 |
| 18. Tenant Public UX Unification | 3/3 | Complete | 2026-04-17 |
| 19. Human Verification Gate Closure | 2/2 | Complete | 2026-04-17 |
| 20. Phase 18 Verification Backfill + UX18 Closure | 1/1 | Complete | 2026-04-17 |

---

## Milestone Summary

**Key Decisions:**
- Close human-only verification gates with explicit UAT evidence artifacts before milestone archival.
- Backfill missing phase verification artifacts when execution summaries exist but verification chain is incomplete.
- Keep requirements traceability synchronized with verification outcomes before closure commits.

**Issues Resolved:**
- Closed ADMU human-verification gates for production admin leads route reachability and readability.
- Closed PAL-01 visual-gate closure through completed route-level palette sweep evidence.
- Backfilled phase 18 verification chain and eliminated UX18 orphaned requirements.

**Issues Deferred:**
- Nyquist validation parity remains partial for phases 14-20 and should be normalized in follow-up validation work.

**Technical Debt Incurred:**
- Phase 18 verification was created post-implementation; future phases should emit verification artifacts immediately at phase close to avoid audit drift.

---

_For current project status, see `.planning/ROADMAP.md`._
