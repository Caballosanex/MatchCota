---
phase: 01-fix-tenant-subdomain-frontend-routing-after-registration
plan: 06
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03, 01-04, 01-05]
files_modified: []
autonomous: false
gap_closure: true
requirements: [UAT-GAP-1, UAT-GAP-2, UAT-GAP-3]

user_setup:
  - service: aws-ec2
    why: "Frontend deploy target — the EC2 instance running nginx that serves the SPA"
    env_vars:
      - name: FRONTEND_HOST
        source: "EC2 public IP or hostname from AWS Console or `terraform -chdir=infrastructure/terraform/environments/prod output`"
      - name: FRONTEND_SSH_KEY_PATH
        source: "Path to the SSH private key for EC2 access (only needed if DEPLOY_TRANSPORT=ssh)"
      - name: AWS_PROFILE
        source: "AWS CLI profile with EC2/SSM access (default: matchcota)"
      - name: KNOWN_TENANT_SLUG
        source: "An existing tenant slug in production DB (e.g., shelter5)"

must_haves:
  truths:
    - "Apex matchcota.tech and tenant {slug}.matchcota.tech render distinct page content (different headers, navigation, branding)"
    - "After registering a new tenant, browser lands on https://{slug}.matchcota.tech/ (not /login)"
    - "Apex matchcota.tech/test returns a redirect (3xx) to /, not HTTP 200"
    - "deploy-frontend.sh --verify-route-contracts-only passes all assertions with zero exit code"
    - "post-deploy-readiness.sh frontend-route-readiness stage passes"
  artifacts:
    - path: "frontend/dist/index.html"
      provides: "Production build of frontend with Phase 01 code changes baked in"
    - path: "frontend/dist/assets/"
      provides: "Compiled JS/CSS bundles containing GuardedRoot, ApexTenantRouteGuard, host-aware PublicLayout, and tenant-root registration redirect"
  key_links:
    - from: "frontend/dist/ (on EC2)"
      to: "nginx try_files"
      via: "deploy-frontend.sh publish_static_assets + configure_nginx_and_verify_script"
      pattern: "try_files.*index.html"
    - from: "deploy-frontend.sh"
      to: "live https://matchcota.tech"
      via: "verify_host_routing_contracts curl assertions"
      pattern: "assert_redirect_to_root"
---

<objective>
Close all 3 HUMAN-UAT gaps by rebuilding and redeploying the frontend to production EC2.

Purpose: Code verification passed 17/17 — all host-aware routing guards, registration redirect logic, and shell distinction are correct in the repository. The 3 UAT failures all trace to one root cause: the deployed frontend bundle on EC2 is stale (built before Phase 01 changes were committed). Redeploying the current code fixes all gaps simultaneously.

Output: Production frontend serving the Phase 01 bundle, with all route contract assertions passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fix-tenant-subdomain-frontend-routing-after-registration/01-CONTEXT.md
@.planning/phases/01-fix-tenant-subdomain-frontend-routing-after-registration/01-HUMAN-UAT.md
@.planning/phases/01-fix-tenant-subdomain-frontend-routing-after-registration/01-04-SUMMARY.md

<interfaces>
<!-- deploy-frontend.sh key contracts the executor needs to understand -->

From infrastructure/scripts/deploy-frontend.sh:
- FRONTEND_HOST (required) — EC2 hostname or IP
- FRONTEND_SSH_KEY_PATH (optional) — SSH key for ssh transport
- DEPLOY_TRANSPORT — auto|ssh|ssm (default: auto)
- KNOWN_TENANT_SLUG — required for route contract verification
- VITE_API_URL defaults to https://api.matchcota.tech
- VITE_BASE_DOMAIN defaults to matchcota.tech
- VITE_ENVIRONMENT defaults to production
- --verify-route-contracts-only flag runs only curl-based route assertions (no build/deploy)

From infrastructure/scripts/post-deploy-readiness.sh:
- KNOWN_TENANT_SLUG or KNOWN_TENANT_HOST required for frontend-route-readiness stage
- AWS_PROFILE defaults to matchcota
- Calls deploy-frontend.sh --verify-route-contracts-only internally
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate frontend production build compiles with Phase 01 code</name>
  <read_first>
    - infrastructure/scripts/deploy-frontend.sh (lines 291-305 — build_frontend function)
    - frontend/package.json (build script definition)
    - frontend/src/App.jsx (confirm GuardedRoot and route guards are present in source)
    - frontend/src/routing/hostRouting.js (confirm host routing module exists)
    - frontend/src/pages/public/RegisterTenant.jsx (confirm buildTenantRootUrl redirect is present)
  </read_first>
  <files>frontend/dist/index.html, frontend/dist/assets/</files>
  <action>
Run a local production build to confirm the current codebase compiles cleanly with the exact same environment variables that deploy-frontend.sh uses:

```bash
cd frontend
VITE_API_URL="https://api.matchcota.tech" \
VITE_BASE_DOMAIN="matchcota.tech" \
VITE_ENVIRONMENT="production" \
npm run build
```

After the build succeeds, verify the compiled bundle contains the Phase 01 code by checking the output:

1. Confirm `frontend/dist/index.html` exists and contains the SPA shell (`<!doctype html>` or `<!DOCTYPE html>`)
2. Confirm at least one JS asset in `frontend/dist/assets/` contains the string `GuardedRoot` or the host routing logic markers (the bundler may mangle names, so also check for `isTenantHost` or `resolveHostContext` or `ApexTenantRouteGuard` or `isApexTenantPathBlocked`)
3. Confirm `buildTenantRootUrl` or its compiled equivalent is present in the JS bundle (search for `matchcota.tech` string in assets to confirm VITE_BASE_DOMAIN was baked in)
4. Run the existing frontend unit tests to confirm no regressions: `cd frontend && npx vitest run`

This task does NOT deploy — it only validates the build is production-ready with Phase 01 code.
  </action>
  <verify>
    <automated>cd /Users/alex/Documents/GITs/MatchCota/frontend && VITE_API_URL="https://api.matchcota.tech" VITE_BASE_DOMAIN="matchcota.tech" VITE_ENVIRONMENT="production" npm run build && test -f dist/index.html && grep -rl "matchcota.tech" dist/assets/*.js | head -1 && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - frontend/dist/index.html exists and is non-empty
    - At least one file in frontend/dist/assets/*.js contains the literal string "matchcota.tech" (proving VITE_BASE_DOMAIN is baked in)
    - `npx vitest run` exits 0 with all tests passing (including hostRouting.test.js and RegisterTenant.test.js)
    - Build command exits 0 with no compilation errors
  </acceptance_criteria>
  <done>Production frontend build compiles cleanly, contains Phase 01 host-routing code, and all unit tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy to production EC2 and verify all 3 UAT gaps are closed</name>
  <read_first>
    - infrastructure/scripts/deploy-frontend.sh (full script — understand env vars and flow)
    - infrastructure/scripts/post-deploy-readiness.sh (readiness orchestration)
    - .planning/phases/01-fix-tenant-subdomain-frontend-routing-after-registration/01-HUMAN-UAT.md (the 3 failing tests to re-verify)
  </read_first>
  <files>infrastructure/scripts/deploy-frontend.sh</files>
  <action>
User deploys the Phase 01 frontend bundle to production EC2 using the existing deploy-frontend.sh script. The script builds, publishes, configures nginx, and runs automated route contract assertions. After automated deploy, user performs manual browser verification of 3 UAT scenarios.

**Step 1: Run the deploy script** (user provides FRONTEND_HOST and credentials):

```bash
cd /Users/alex/Documents/GITs/MatchCota

# Set required env vars (substitute actual values):
export FRONTEND_HOST="<your-ec2-ip-or-hostname>"
export KNOWN_TENANT_SLUG="shelter5"
export AWS_PROFILE="matchcota"

# If using SSH transport:
export FRONTEND_SSH_KEY_PATH="<path-to-your-ssh-key>"
export DEPLOY_TRANSPORT="ssh"

# If using SSM transport (no SSH key needed):
export DEPLOY_TRANSPORT="ssm"

# Run the full deploy:
bash infrastructure/scripts/deploy-frontend.sh
```

The script will: install deps, build with production env vars, publish to EC2, configure nginx, and run all route contract assertions. It should exit 0 with `[deploy-frontend] deploy complete`.

**Step 2: Run post-deploy readiness** to confirm the full readiness chain passes:

```bash
KNOWN_TENANT_SLUG="shelter5" AWS_PROFILE="matchcota" \
  bash infrastructure/scripts/post-deploy-readiness.sh
```

Should print `[post-deploy-readiness] stage=frontend-route-readiness pass` and `[post-deploy-readiness] stage=complete pass`.

**Step 3: Manual browser verification of the 3 UAT scenarios:**

1. **UAT Gap 1 — Shell distinction:** Open `https://matchcota.tech/` and `https://shelter5.matchcota.tech/` in separate incognito tabs. Apex should show the marketing landing with "Registrar Protectora" CTA. Tenant subdomain should show the tenant-branded public shell with shelter name and adopter-focused navigation (no registration CTA).

2. **UAT Gap 2 — Registration handoff:** Go to `https://matchcota.tech/register-tenant`, register a new shelter with a unique slug (e.g., `shelter6`). After success confirmation, browser should redirect to `https://shelter6.matchcota.tech/` (tenant root), NOT to `/login`.

3. **UAT Gap 3 — Apex route guard:** Visit `https://matchcota.tech/test` in a browser. It should redirect to `https://matchcota.tech/` (the apex root), NOT show a 200 response with test content.
  </action>
  <verify>
    <automated>KNOWN_TENANT_SLUG="shelter5" VITE_BASE_DOMAIN="matchcota.tech" VITE_ENVIRONMENT="production" bash /Users/alex/Documents/GITs/MatchCota/infrastructure/scripts/deploy-frontend.sh --verify-route-contracts-only</automated>
  </verify>
  <acceptance_criteria>
    - deploy-frontend.sh exits 0 and prints "deploy complete"
    - post-deploy-readiness.sh exits 0 with all 4 stages passing (dns, tls, api, frontend-route-readiness)
    - Browser shows different content for matchcota.tech vs {slug}.matchcota.tech
    - New tenant registration redirects to tenant root (not /login)
    - matchcota.tech/test redirects to matchcota.tech/ (not HTTP 200)
  </acceptance_criteria>
  <done>All 3 HUMAN-UAT gaps closed: apex and tenant shells are visually distinct in production, registration handoff lands on tenant root, and apex route guard correctly redirects tenant-only paths. deploy-frontend.sh and post-deploy-readiness.sh both exit 0.</done>
  <resume-signal>Type "approved" if all 3 UAT gaps pass, or describe which gap(s) still fail with observed behavior.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local build -> EC2 deploy | Build artifacts cross from dev machine to production server via SSH/SSM |
| nginx -> SPA bundle | nginx serves the SPA with try_files; all routing is client-side |
| curl assertions -> production hosts | Deploy script validates live behavior via HTTPS requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-01 | Tampering | dist/ archive in transit | mitigate | deploy-frontend.sh uses rsync over SSH (encrypted) or S3+SSM (IAM-scoped); nginx config is generated on-host, not uploaded |
| T-01-06-02 | Spoofing | Route contract assertions | accept | curl assertions run against known production domains over HTTPS with valid certs; DNS/TLS already verified by earlier readiness stages |
| T-01-06-03 | Denial of Service | Stale bundle re-served | mitigate | nginx reload + rm -rf docroot before rsync ensures no stale file caching; verify_public_route_contract checks SPA shell is served |
</threat_model>

<verification>
All 3 HUMAN-UAT gaps from 01-HUMAN-UAT.md are addressed:
1. Host-aware shell distinction visible on live domain (deploy-frontend.sh verify_host_routing_contracts checks apex vs tenant content markers)
2. Registration handoff lands on tenant root (RegisterTenant.jsx buildTenantRootUrl is in the deployed bundle)
3. Frontend readiness contract gate passes (post-deploy-readiness.sh frontend-route-readiness stage invokes deploy-frontend.sh --verify-route-contracts-only)
</verification>

<success_criteria>
- deploy-frontend.sh completes with exit 0 and all contract assertions pass
- post-deploy-readiness.sh completes with exit 0 including frontend-route-readiness stage
- User confirms via browser that all 3 UAT scenarios pass (shell distinction, registration handoff to tenant root, apex route guard redirect)
- 01-HUMAN-UAT.md can be updated to status: passed with all 3 tests marked [pass]
</success_criteria>

<output>
After completion, create `.planning/phases/01-fix-tenant-subdomain-frontend-routing-after-registration/01-06-SUMMARY.md`
</output>
